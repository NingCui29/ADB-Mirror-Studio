# ADB Mirror Studio V1.7.0 真机验收

测试设备：`10.67.116.12:5555`，Android 12，Mali GPU，Windows x64 桌面客户端。

| 项目 | 结果 |
|---|---|
| 内存当前占用 | `MemTotal=8104732 kB`、`MemAvailable=1681868 kB` 时，ADB 计算与界面均为 79.2% |
| 内存容量 | 界面显示“已用 6.1 / 7.7 GiB”，与 `/proc/meminfo` 换算一致 |
| 30 秒平均值 | 连续采样显示约 79.3%，有效样本随 30 秒窗口滚动 |
| 设备切换 | 自动化测试确认切换后清空旧读数，旧设备在途结果不会覆盖新设备 |
| 最大化窗口 | CPU、GPU、内存三列完整显示，无重叠或裁切 |
| 600×650 窄窗口 | 三列当前值、30 秒平均值及容量可读，无横向裁切 |
| 便携包启动 | 从最终 ZIP 独立解压后启动成功；界面版本为 `V1.7.0`，当前设备与内存读数正常显示 |

本次验收覆盖新增内存监测、相关响应式布局和便携包启动；未重复执行 V1.6.0 的完整镜像、录屏、传输和安装矩阵。
